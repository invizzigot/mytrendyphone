services:
  - type: web
    name: vue-app
    runtime: node
    buildCommand: npm run build
    startCommand: npm run start
    envVars:
      - key: BACKEND_URL
        value: https://image-backend-zvjc.onrender.com
